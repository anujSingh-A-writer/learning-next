/* import React from 'react'

interface FormFie

const Form = ({formfieldsObject, submitActionHandler}) => {
  return (
    <form onSubmit={submitAction}>
        
    </form>
  )
}

export default Form */
